<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.onfhir</groupId>
    <artifactId>onfhir-libs-parent</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>

    <name>onFHIR Reusable Libraries</name>
    <description>Reusable, transport-neutral libraries for working with HL7 FHIR.</description>
    <url>https://github.com/srdc/onfhir-libs</url>
    <inceptionYear>2019</inceptionYear>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>tnamli</id>
            <name>Tuncay Namli</name>
            <email>tuncay@srdc.com.tr</email>
            <organization>SRDC Corp.</organization>
            <organizationUrl>https://www.srdc.com.tr</organizationUrl>
        </developer>
        <developer>
            <id>msfyuksel</id>
            <name>Mustafa Yuksel</name>
            <email>mustafa@srdc.com.tr</email>
            <organization>SRDC Corp.</organization>
            <organizationUrl>https://www.srdc.com.tr</organizationUrl>
        </developer>
        <developer>
            <id>sinaci</id>
            <name>Ali Anil Sinaci</name>
            <email>anil@srdc.com.tr</email>
            <organization>SRDC Corp.</organization>
            <organizationUrl>https://www.srdc.com.tr</organizationUrl>
        </developer>
        <developer>
            <id>postaci</id>
            <name>Senan Postaci</name>
            <email>senan@srdc.com.tr</email>
            <organization>SRDC Corp.</organization>
            <organizationUrl>https://www.srdc.com.tr</organizationUrl>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/srdc/onfhir-libs.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/srdc/onfhir-libs.git</developerConnection>
        <url>https://github.com/srdc/onfhir-libs</url>
        <tag>HEAD</tag>
    </scm>
    <issueManagement>
        <system>GitHub</system>
        <url>https://github.com/srdc/onfhir-libs/issues</url>
    </issueManagement>

    <modules>
        <module>onfhir-common</module>
        <module>onfhir-client</module>
        <module>onfhir-path</module>
        <module>onfhir-query</module>
        <module>onfhir-config</module>
        <module>onfhir-expression</module>
        <module>onfhir-validation</module>
        <module>onfhir-template-engine</module>
        <module>onfhir-definitions-r4</module>
        <module>onfhir-definitions-r5</module>
        <module>onfhir-definitions-stu3</module>
        <module>onfhir-r4</module>
        <module>onfhir-r5</module>
        <module>onfhir-stu3</module>
        <module>onfhir-libs-bom</module>
    </modules>

    <properties>
        <revision>4.0.0</revision>
        <onfhir.libs.version>${revision}</onfhir.libs.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <maven.compiler.release>11</maven.compiler.release>
        <library.gates.skip>false</library.gates.skip>

        <scala.version>2.13.16</scala.version>
        <scala.binary.version>2.13</scala.binary.version>
        <scala.pcombinators>2.4.0</scala.pcombinators>
        <!-- Pinned to the json4s version Apache Spark 3.5.x bundles. json4s
             types are part of this library's public API (io.onfhir.api.Resource
             is a json4s JObject), and Spark consumers such as spark-on-fhir
             would hit a classpath conflict on any other line. Do not bump
             without a matching Spark version change. -->
        <json4s.version>3.7.0-M11</json4s.version>
        <!-- json4s 3.7.0-M11 resolves jackson-databind 2.12.2 transitively,
             which is affected by CVE-2020-36518 (deeply nested JSON exhausts
             the stack) - reachable here because these libraries parse
             untrusted FHIR JSON. Overridden to the version Spark 3.5.x
             bundles, which spark-on-fhir also declares, so the pin closes the
             CVE and matches the Spark classpath instead of fighting it.
             json4s only needs a compatible Jackson 2.x, so this is
             independent of the json4s pin above. -->
        <jackson.version>2.15.2</jackson.version>
        <scala.xml.version>2.2.0</scala.xml.version>
        <slf4j.version>2.0.18</slf4j.version>
        <config.version>1.4.3</config.version>
        <specs2.version>4.20.4</specs2.version>
        <nimbus-jose.version>9.37.3</nimbus-jose.version>
        <nimbus-oidc.version>11.9.1</nimbus-oidc.version>
        <apache-commons-lang.version>3.17.0</apache-commons-lang.version>
        <apache-commons-io.version>2.18.0</apache-commons-io.version>
        <apache-commons-text.version>1.13.0</apache-commons-text.version>
        <reflections.version>0.10.2</reflections.version>

        <scala.maven.plugin.version>4.8.1</scala.maven.plugin.version>
        <jar.plugin.version>3.3.0</jar.plugin.version>
        <surefire.plugin.version>3.2.5</surefire.plugin.version>
        <compiler.plugin.version>3.12.1</compiler.plugin.version>
        <resources.plugin.version>3.3.1</resources.plugin.version>
        <source.plugin.version>3.2.1</source.plugin.version>
        <antrun.plugin.version>3.1.0</antrun.plugin.version>
        <gpg.plugin.version>3.2.8</gpg.plugin.version>
        <!-- Pinned because Maven 3.8.x binds maven-deploy-plugin 2.7 by
             default, which has no deployAtEnd: it uploads module by module,
             so a failure part way through the reactor can leave a partially
             published version in a repository that forbids redeploy. The
             pin also fixes which -DaltDeploymentRepository syntax the
             release runbook must use - 3.x dropped layout support, so it
             takes "id::url" and rejects the "id::layout::url" form that 2.7
             requires. The two are mutually exclusive; see RELEASING.md
             section 2. -->
        <deploy.plugin.version>3.1.2</deploy.plugin.version>
        <enforcer.plugin.version>3.6.3</enforcer.plugin.version>
        <flatten.plugin.version>1.6.0</flatten.plugin.version>
        <central.publishing.plugin.version>0.8.0</central.publishing.plugin.version>
    </properties>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>net.alchim31.maven</groupId>
                    <artifactId>scala-maven-plugin</artifactId>
                    <version>${scala.maven.plugin.version}</version>
                    <configuration>
                        <scalaCompatVersion>${scala.binary.version}</scalaCompatVersion>
                        <encoding>UTF-8</encoding>
                        <args>
                            <arg>-deprecation</arg>
                            <arg>-feature</arg>
                        </args>
                    </configuration>
                    <executions>
                        <execution>
                            <id>scala-compile-first</id>
                            <phase>process-resources</phase>
                            <goals>
                                <goal>add-source</goal>
                                <goal>compile</goal>
                            </goals>
                        </execution>
                        <execution>
                            <id>scala-test-compile</id>
                            <phase>process-test-resources</phase>
                            <goals>
                                <goal>testCompile</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${jar.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${surefire.plugin.version}</version>
                    <configuration>
                        <includes>
                            <include>**/*Spec.*</include>
                            <include>**/When*.*</include>
                            <include>**/*Test.*</include>
                            <include>**/*Suite.*</include>
                        </includes>
                        <runOrder>alphabetical</runOrder>
                        <reuseForks>true</reuseForks>
                        <forkCount>1</forkCount>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${compiler.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${resources.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${source.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-gpg-plugin</artifactId>
                    <version>${gpg.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>${deploy.plugin.version}</version>
                    <configuration>
                        <!-- All coordinates release together, so the whole
                             reactor uploads as one step or not at all. -->
                        <deployAtEnd>true</deployAtEnd>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>${enforcer.plugin.version}</version>
                <executions>
                    <execution>
                        <id>ban-actor-frameworks</id>
                        <phase>validate</phase>
                        <goals><goal>enforce</goal></goals>
                        <configuration>
                            <skip>${library.gates.skip}</skip>
                            <rules>
                                <bannedDependencies>
                                    <searchTransitive>true</searchTransitive>
                                    <excludes>
                                        <exclude>com.typesafe.akka:*</exclude>
                                        <exclude>org.apache.pekko:*</exclude>
                                    </excludes>
                                    <message>onFHIR reusable libraries must not resolve Akka or Pekko dependencies.</message>
                                </bannedDependencies>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <id>package-license-files</id>
                        <phase>process-resources</phase>
                        <goals><goal>copy-resources</goal></goals>
                        <configuration>
                            <outputDirectory>${project.build.outputDirectory}/META-INF</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${maven.multiModuleProjectDirectory}</directory>
                                    <filtering>false</filtering>
                                    <includes>
                                        <include>LICENSE</include>
                                        <include>NOTICE</include>
                                    </includes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <version>${flatten.plugin.version}</version>
                <configuration>
                    <flattenMode>oss</flattenMode>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals><goal>flatten</goal></goals>
                    </execution>
                    <execution>
                        <id>flatten.clean</id>
                        <goals><goal>clean</goal></goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <phase>package</phase>
                                <goals><goal>jar-no-fork</goal></goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>net.alchim31.maven</groupId>
                        <artifactId>scala-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>attach-scaladocs</id>
                                <phase>package</phase>
                                <goals><goal>doc-jar</goal></goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals><goal>sign</goal></goals>
                                <configuration>
                                    <gpgArguments>
                                        <arg>--pinentry-mode</arg>
                                        <arg>loopback</arg>
                                    </gpgArguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>central</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>${central.publishing.plugin.version}</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>false</autoPublish>
                            <waitUntil>validated</waitUntil>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.scala-lang</groupId>
                <artifactId>scala-library</artifactId>
                <version>${scala.version}</version>
            </dependency>
            <dependency>
                <groupId>org.scala-lang</groupId>
                <artifactId>scala-reflect</artifactId>
                <version>${scala.version}</version>
            </dependency>
            <dependency>
                <groupId>org.scala-lang.modules</groupId>
                <artifactId>scala-parser-combinators_${scala.binary.version}</artifactId>
                <version>${scala.pcombinators}</version>
            </dependency>
            <dependency>
                <groupId>org.json4s</groupId>
                <artifactId>json4s-jackson_${scala.binary.version}</artifactId>
                <version>${json4s.version}</version>
            </dependency>
            <dependency>
                <groupId>org.json4s</groupId>
                <artifactId>json4s-xml_${scala.binary.version}</artifactId>
                <version>${json4s.version}</version>
            </dependency>
            <!-- Imported so every Jackson artifact json4s pulls in resolves at
                 one coherent version. See the jackson.version property. -->
            <dependency>
                <groupId>com.fasterxml.jackson</groupId>
                <artifactId>jackson-bom</artifactId>
                <version>${jackson.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.scala-lang.modules</groupId>
                <artifactId>scala-xml_${scala.binary.version}</artifactId>
                <version>${scala.xml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${apache-commons-lang.version}</version>
            </dependency>
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${apache-commons-io.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-text</artifactId>
                <version>${apache-commons-text.version}</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>com.typesafe</groupId>
                <artifactId>config</artifactId>
                <version>${config.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nimbusds</groupId>
                <artifactId>nimbus-jose-jwt</artifactId>
                <version>${nimbus-jose.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nimbusds</groupId>
                <artifactId>oauth2-oidc-sdk</artifactId>
                <version>${nimbus-oidc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.specs2</groupId>
                <artifactId>specs2-core_${scala.binary.version}</artifactId>
                <version>${specs2.version}</version>
            </dependency>
            <dependency>
                <groupId>org.specs2</groupId>
                <artifactId>specs2-junit_${scala.binary.version}</artifactId>
                <version>${specs2.version}</version>
            </dependency>
            <dependency>
                <groupId>org.reflections</groupId>
                <artifactId>reflections</artifactId>
                <version>${reflections.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
