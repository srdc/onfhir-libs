# Maven coordinate=SPDX identifier
#
# Use only when a dependency's published POM omits or misstates its license.
# Record the evidence in a comment above each entry and re-review on upgrade.
#
# (Currently empty: the ANTLR 3.3 runtime entry was removed when the unused
# org.antlr:stringtemplate dependency that pulled it in was dropped from
# onfhir-path.)
